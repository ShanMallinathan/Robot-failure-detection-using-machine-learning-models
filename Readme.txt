This model is trained on a UCI dataset

The folder contains:

5 ipynb scripting files - one for each failure models

i .py file - for extraction and encoding of the label values
(Note, the program will run only on properly delimited csv files)
(Also new paths needs to be specified)

1 exports folder containing all the ANN models, weights, html 
notebook files in line with the scores specified in the report. 

Data_files is where the refined encoded data is stored and all
ipynb files are pointing it directly. Should therebe any issue, 

Folder X contains all force torque values
Folder Y contains all encoded label values
